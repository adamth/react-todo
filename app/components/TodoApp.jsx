const React = require('react');

var TodoApp = React.createClass({
    render: function () {
        return (
            <div>
                Sorry I can't hang tonight
            </div>
        );
    }
});

module.exports = TodoApp;